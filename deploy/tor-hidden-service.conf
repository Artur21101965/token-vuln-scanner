# Tor Hidden Service — Monster Exploit Scanner
# Добавить в /etc/tor/torrc:

HiddenServiceDir /var/lib/tor/monster_scanner/
HiddenServicePort 8000 127.0.0.1:8000

# После добавления перезапустить Tor:
#   systemctl restart tor
#
# .onion адрес будет в:
#   cat /var/lib/tor/monster_scanner/hostname
